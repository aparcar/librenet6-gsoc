# LibreNet6 (GSoC 2018)

## Goal

Supplying IPv6 addresses to networks which else wouldn't have public IPv6

## Current Approach

* Use central Tinc server which manages public IPv6 addresses.
* Manually configured subnets are assigned to nodes.
* Babel is used (#1)
